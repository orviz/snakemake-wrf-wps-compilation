# =========================================================================
# SNAKEMAKE & EASYBUILD AUTOMATED PIPELINE METADATA (Local fallbacks)
# =========================================================================

# Snakemake internal database, lockfiles, and rule logs
.snakemake/

# Dynamic EESSI database discovery footprints via eb -S (Cluster-dependent JSONs)
audit/

# Local target binary directory hosting deployment symlinks
bin/

# Temporary data downloads, tarballs, and runtime outputs
data/
logs/
